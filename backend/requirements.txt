# Backend PWA MUGEN Hair Co.
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
# BUGFIX (lihat CHANGELOG di README.md): hashing password sebelumnya lewat
# passlib[bcrypt], yang TIDAK kompatibel dengan bcrypt>=4.0 dan sempat
# "diperbaiki" dengan mengunci bcrypt<4.0 -- tapi pin itu rapuh (tidak
# menolong kalau environment lokal sudah lebih dulu punya bcrypt versi
# baru terpasang). auth_db.py sekarang memanggil library `bcrypt` LANGSUNG
# tanpa passlib, jadi passlib tidak lagi dibutuhkan sama sekali, dan versi
# bcrypt berapa pun (lama maupun baru) sama-sama berfungsi.
bcrypt>=4.0
python-multipart>=0.0.9
itsdangerous>=2.1.2

# TAHAP migrasi PostgreSQL (Neon): dipakai db_compat.py HANYA kalau environment
# variable DATABASE_URL diisi -- kalau kosong (default, jalur SQLite), library
# ini tidak pernah diimpor sama sekali. Tetap dicantumkan di sini (bukan
# opsional) supaya siap dipakai begitu Owner mengisi DATABASE_URL, tanpa perlu
# instalasi ulang.
psycopg2-binary>=2.9.9

# Download Laporan PDF (Menu Backup) -- lihat laporan_pdf.py.
reportlab>=4.0.0

# Storage Cloudflare R2 (S3-compatible) -- dipakai r2_storage.py HANYA kalau
# environment variable R2_* lengkap diisi (pola sama seperti psycopg2-binary
# di atas: kalau kosong, library ini tetap terpasang tapi tidak pernah
# dipanggil, jalur lama BLOB-di-database tetap berfungsi apa adanya).
boto3>=1.34.0

# Pembatasan Slot Jam Booking (booking_db.py, WIB = ZoneInfo("Asia/Jakarta")):
# zoneinfo (stdlib sejak Python 3.9) butuh database IANA tzdata di sistem --
# tidak semua image OS/container menyediakannya bawaan. Paket tzdata ini
# (murni data, dari PyPI) memastikan ZoneInfo("Asia/Jakarta") selalu berhasil
# di server manapun (termasuk Render), tidak bergantung tzdata sistem.
tzdata>=2024.1

# FONDASI Multi-Tenant Phase 4 (Billing & Payment Gateway): dipakai
# gateway_client_base.py (dipakai billing_gateway_client.py & payment_gateway_client.py)
# untuk memanggil REST API Payment Gateway (buat transaksi, cek status).
# HANYA dipanggil kalau kredensial provider sudah diisi Super Admin lewat
# UI (pola sama seperti boto3/psycopg2-binary di atas -- endpoint Billing/
# Booking balas 503 dengan pesan jelas kalau kredensial belum diisi, TIDAK
# pernah crash saat boot).
requests>=2.31.0

# Modul BARU Absensi: Export Excel Laporan Absensi (lihat attendance_excel.py)
# -- Laporan lain di aplikasi ini HANYA punya export PDF (reportlab), Excel
# ATAS PERMINTAAN EKSPLISIT spesifikasi Absensi ("Export -> Excel, PDF"),
# pemakaian PERTAMA library ini di proyek.
openpyxl>=3.1.0

# FITUR Notifikasi Push (Web Push/VAPID, termasuk iPhone lewat PWA "Add to
# Home Screen" sejak iOS 16.4) -- dipakai push_service.py HANYA kalau
# environment variable VAPID_PRIVATE_KEY/VAPID_PUBLIC_KEY diisi (pola sama
# seperti boto3/psycopg2-binary di atas: kalau kosong, library ini tetap
# terpasang tapi tidak pernah dipanggil jaringan, endpoint /api/push/*
# tetap berfungsi normal -- HANYA kirim_ke_user()/kirim_ke_role() yang jadi
# no-op, TIDAK pernah crash saat boot).
pywebpush>=2.0.0

